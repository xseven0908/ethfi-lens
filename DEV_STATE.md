当前目标是让 Token Lens 的 ETHFI、BP、PENDLE、HYPE 四币首页能以图表为主快速判断优势并保留详情下钻；截至 2026-09-02，根路径 `/` 为四币对比首页，详情入口仍为 `?token=ethfi|bp|pendle|hype`，本轮已移除 CNY 切换并将前端固定为 USD 口径，金额只在页头和矩阵说明标记一次单位、数值不重复显示 `$`/`US$`/`￥`，同时强化“当前优势一览”主视觉，新增四币起点归一为 100 的 30D 相对价格曲线，保留五维优势条形图，并将未来事件与持续压力由长段文字改为横向表格；关键决策仍是不设置总分、缺失数据不按 0 处理、只对同口径市场指标排名，sETHFI 质押资产、BP 现货量、Pendle TVL、HyperCore 永续量只作各自业务参照。核心实现位于 `app/compare-dashboard.tsx`，页面入口和单一币种口径在 `app/page.tsx`，视觉层级在 `app/globals.css`，四个详情页的金额格式分别在 `app/page.tsx`、`app/bp-dashboard.tsx`、`app/pendle-dashboard.tsx`、`app/hype-dashboard.tsx`，回归检查在 `tests/rendered-html.test.mjs`；已验证 `npm run lint` 通过、`npm test` 完成 Vinext 生产构建且 2/2 测试通过。已知问题是实时模块依赖公共外部 API、ETHFI 解锁日历为代码内维护且会过期、本轮未做浏览器视觉与触控检查；既往 CoinGecko/Binance 不可用的方案已由 CoinPaprika/OKX 降级路径替代。下一步是做桌面和移动端人工视觉验收，并补充曲线数据对齐、接口降级、缺失值和排名边界测试。
