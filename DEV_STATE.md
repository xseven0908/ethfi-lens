当前目标是维护 Token Lens 的 ETHFI、BP、PENDLE 三资产中文研究看板，使市场、供应、质押/退出、协议收入与收益市场均使用可核验数据并在源异常时按模块降级；截至 2026-09-01，`main` 基于 `7d477cc` 存在本轮未提交改动，原有 ETHFI 四链 sETHFI/退出队列/回购和 BP Backpack/Solana 看板保持不变，本轮已在 `app/page.tsx` 增加 PENDLE 资产切换及独立青绿色主题，在 `app/pendle-dashboard.tsx` 完成价格趋势、自动摘要、活跃市场 TVL 与链分布、sPENDLE 质押和收入回购周期、7/30/90 天到期集中度、供应排放及头部收益市场表，并新增 `app/api/pendle/route.ts` 聚合 Binance/OKX/Bybit、CoinGecko 和 Pendle `/v1/spendle/data`、`app/api/pendle-markets/route.ts` 分页聚合官方 `/v2/markets/all`；关键决策是只统计未到期且 TVL 大于 0 的 Pendle 市场、将行情与协议市场独立降级、以 sPENDLE 为当前治理和价值捕获主口径而仅单列遗留虚拟 vePENDLE、不再展示已完成的团队/投资人解锁倒计时、不把缺失周期值当作零，协议市场聚合缓存 5 分钟且所有金额随 USD/CNY 切换；元数据、README 和 `public/og-brand-v4.png` 已更新为三资产版本，失效的 starter skeleton 测试已替换，实测 `npm run lint` 无告警，`npm test` 完成 Vinext 生产构建并 2/2 通过，构建生成首页和七个 API 路由，另外通过构建后的 Worker 实测 `/api/pendle` 返回行情与 sPENDLE、`/api/pendle-markets` 返回 71 个活跃市场和 12 个头部市场；当前已知限制是 PENDLE 页面依赖公共交易所、CoinGecko 与 Pendle Core API，浏览器预览窗口在本会话不可用但不影响构建和接口验证，Boros 尚未并入以避免在收入归属未确认前与 Pendle V2 混算，下一步应在真实浏览器补做桌面/移动端视觉与交互检查，并为 PENDLE 外部源失败、分页截断、过期市场过滤和周期数据缺失增加行为级测试。
