当前目标是把 Token Lens 建成 ETHFI、BP、PENDLE、HYPE 四币可横向判断优势且可下钻详情的研究看板；截至 2026-09-02，根路径 `/` 已改为四币对比首页，原详情入口保留为 `?token=ethfi|bp|pendle|hype`，首页已完成动态领先项、11 项统一指标矩阵、市场流动性/30D 动能/供应释放/质押深度/数据完整度优势图、FDV 与各自业务参照、价值回流证据及未来 90 天事件和持续压力，关键决策是不设置武断总分、缺失数据不按 0 处理、只对同口径市场指标排名且不拿 sETHFI 质押资产、BP 现货量、Pendle TVL、HyperCore 永续量跨业务排名；核心实现位于 `app/compare-dashboard.tsx`，入口与路由在 `app/page.tsx`，样式在 `app/globals.css`，分享元数据在 `app/layout.tsx`，回归检查在 `tests/rendered-html.test.mjs`。已验证 `npm run lint` 通过、`npm test` 完成 Vinext 生产构建且 2/2 测试通过，本地首页及 `/api/market`、`/api/chain`、`/api/buyback`、`/api/bp`、`/api/pendle`、`/api/pendle-markets`、`/api/hype` 均返回 HTTP 200 和有效载荷；已知问题是实时模块依赖公共外部 API、ETHFI 已知解锁日历目前为代码内维护且会随时间过期，本轮没有做浏览器视觉与触控检查，CoinGecko/Binance 在既往本地验证中曾不可用，现有 CoinPaprika/OKX 降级路径因此保留。下一步是发布当前版本，随后做桌面与移动端视觉验收，并为四币接口降级、指标单位换算、缺失值和排名边界补充行为级测试。
